@model TypeViewModel
@using NavneVelger.Views.Panini
@{
    ViewData["Title"] = "DeleteType";
    ViewData.AddActivePaniniPage(ManagePaniniPages.DeleteType);
}

<h2>Slett</h2>

<h3>Er du sikker?</h3>
<div>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayFor(model => model.Navn)
        </dt>
        <dd>
            <form asp-action="DeleteType">
                <input type="hidden" asp-for="Id" />
                <input type="submit" value="Slett" class="btn btn-default" /> |
                <a asp-action="Type">Back to List</a>
            </form>

        </dd>
    </dl>
</div>
